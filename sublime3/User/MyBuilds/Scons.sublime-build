{
   "cmd": ["scons", "-j2", "debug=1"],
   "file_regex": "^(..[^:]*):([0-9]+):?([0-9]+)?:? (.*)$",
   "working_dir": "${project_path:${folder:${file_path}}}",
   "variants":
   [
      {
         "name": "Clean",
         "cmd": ["scons", "-c"]
      }
   ]
}
